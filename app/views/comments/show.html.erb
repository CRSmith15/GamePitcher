<h2><%= @comment.text%> - <%= @comment.user.username%></h2>

<% if @comment.user == current_user %>
  <%= link_to "Edit Comment", edit_comment_path(@comment)%>
<% end %>

<h4>See the game pitch here: <%= link_to @comment.game.title, game_path(@comment.game_id) %></h4>